{
  "name": "@zerofire/create-cocos-plugin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "preinstall": "npx only-allow pnpm -y",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "deploy": "pnpm run deploy --parallel ",
    "build": "pnpm run build --parallel",
    "avmd": "pnpm changeset add",
    "lint": "eslint --fix packages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@antfu/eslint-config": "^0.25.2",
    "@changesets/cli": "^2.22.0",
    "eslint": "^8.18.0",
    "rollup": "^2.74.1",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "@types/jest": "^27.5.2",
    "jest": "^27.5.1",
    "vitest": "^0.16.0"
  }
}
